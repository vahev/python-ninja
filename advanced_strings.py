# The advanced methods for Strings
src_text = 'Hello Python!!'  # The source text

# Capitalizes the given text
cap_text = src_text.capitalize()

# Makes capital all the letters in a text
upp_text = src_text.upper()

# Makes normal all the letters in a cap_text
low_text = src_text.lower()

# Counts how many matching letters with the argument are in given text
cnt_text = src_text.count('e')

# Find the given argument into a Strings
fnd_text = src_text.find('o')

# Centers a given text putting a given String at beggining and ending
centered_text = src_text.center('*')

# And the methods thata are the respective interfaces for all above
